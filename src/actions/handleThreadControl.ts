import { MessengerContext } from 'bottender';

export const HandlePassThreadControl = async (context: MessengerContext) => {};
export const HandleTakeThreadControl = async (context: MessengerContext) => {};
export const HandleRequestThreadControl = async (context: MessengerContext) => {};
